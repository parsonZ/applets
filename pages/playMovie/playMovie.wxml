<view class="container">
  <block wx:if="{{movieDetails.trailer_urls.length}}">
    <video id="video" src="{{url}}"></video>
  </block>
  <block wx:else>
    <view class="tips">地址解析错误</view>
  </block>
</view>